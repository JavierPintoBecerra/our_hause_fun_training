import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.funtraining.app',
  appName: 'fun_traning',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
